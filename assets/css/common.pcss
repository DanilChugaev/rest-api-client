@import "assets/css/vars/media.pcss";

.container {
  width: 100%;
  max-width: var(--size-container-width);
  padding: var(--spacer-d);

  @media (--max-media-laptop) {
    padding: var(--spacer-d);
  }
}
